export interface ICssStyles {
    styleTag: string;           // css actual style tag
    pmStyleProperty: string;    // internal name for style tag used to referenec a property on textArea or                                      ImageArea
}