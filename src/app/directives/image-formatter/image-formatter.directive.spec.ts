import { ImageFormatterDirective } from './image-formatter.directive';

describe('ImageFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
